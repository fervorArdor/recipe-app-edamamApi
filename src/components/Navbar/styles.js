import { makeStyles } from '@material-ui/styles'

export default makeStyles({
    displayFlex: {
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
    },
})